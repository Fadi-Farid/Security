
# Ignore low-value findings
10010 IGNORE   # Cookie No HttpOnly Flag
10021 WARN     # X-Content-Type-Options Header Missing

# Medium → warn only
# 40018 WARN     # SQL Injection (may vary by plugin)

# High / Critical → FAIL (default behavior, no rule needed)
``
